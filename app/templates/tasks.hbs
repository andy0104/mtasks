{{outlet}}
<h1>My Tasks</h1>
{{#each model as |task|}}
    <div class="well">
        <h4>{{#link-to 'tasks.edit' task.id}}{{task.title}}{{/link-to}}</h4>
        <small>Due: {{format-date task.date 'MM/DD/YYYY'}}</small>
        <p>{{task.description}}</p>
    </div>
{{/each}}
